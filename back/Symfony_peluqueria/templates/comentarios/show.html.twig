{% extends 'base.html.twig' %}

{% block title %}Comentarios{% endblock %}

{% block body %}
    <h1>Comentarios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comentario.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ comentario.descripcion }}</td>
            </tr>
            <tr>
                <th>Valoracion</th>
                <td>{{ comentario.valoracion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comentarios_index') }}">back to list</a>

    <a href="{{ path('app_comentarios_edit', {'id': comentario.id}) }}">edit</a>

    {{ include('comentarios/_delete_form.html.twig') }}
{% endblock %}
